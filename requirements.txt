# 파일 경로: requirements.txt
# BSS Queue-Based Load Leveling 패턴 구현을 위한 Python 의존성

# 웹 프레임워크
fastapi==0.104.1
uvicorn[standard]==0.24.0

# 메시지 큐
pika==1.3.2

# HTTP 클라이언트
requests==2.31.0
httpx==0.25.2

# 데이터 검증 및 직렬화
pydantic==2.5.0

# 로깅 및 모니터링
prometheus-client==0.19.0

# 유틸리티
python-multipart==0.0.6
python-dateutil==2.8.2

# 테스트 (개발용)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# 개발 도구 (선택적)
black==23.11.0
flake8==6.1.0
isort==5.12.0

# Kubernetes Python 클라이언트 (모니터링용)
kubernetes==28.1.0

# 기타 유틸리티
asyncio-mqtt==0.13.0  # MQTT 지원 (필요시)
aiofiles==23.2.1      # 비동기 파일 I/O